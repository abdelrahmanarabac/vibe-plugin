# 🛸 VIBE.AI | System Manifesto & Architecture
## الرؤية التقنية الهندسية لنظام Vibe المتطور

يا هندسة، نظام **Vibe** مش مجرد بلاقين فجما عادي، ده عبارة عن **Design Engineering Operating System** متكامل مبني على مبادئ الـ **Clean Architecture** والـ **Screaming Architecture**. النظام متصمم عشان يشتغل كـ "عقل" بيترجم لغة المصممين لخوارزميات هندسية دقيقة.

---

### 🏛️ 1. المبدأ المعماري: "Screaming Architecture"
النظام متقسم طبقات (Layers) مفصولة تماماً عن بعضها لضمان الاستقرار (Stability) والقابلية للتوسع (Scalability):
- **Core (اللب):** فيه الـ Business Logic الصافي، ميعرفش حاجة عن فجما ولا عن الـ UI.
- **Modules (النظم الوظيفية):** كل ميزة (Feature) ليها عالمها الخاص (AI, Governance, Tokens).
- **Infra (البنية التحتية):** الطبقة اللي بتكلم Figma API أو أي مصادر خارجية.
- **UI (الواجهة):** مبنية بـ React و Tailwind مع نظام **Anti-Gravity** الجمالي.

---

### 🧠 2. العقل المدبر: `AgentCore`
ده المايسترو اللي بيحرك كل حاجة. بيعتمد على حلقة تكرارية تسمى **"Thinking -> Acting -> Healing Loop"**:
1.  **Thinking:** بياخد المدخلات ويبني "Prompt" هندسي دقيق.
2.  **Acting:** بيشغل الذكاء الاصطناعي عشان يولد الـ Tokens المطلوبة.
3.  **Healing:** لو الـ AI غلط في كود أو قيمة، النظام فيه خاصية **Self-Healing** بتصلح الأخطاء تلقائياً قبل ما توصل ليك.

---

### 🌪️ 3. الخوارزميات الأساسية (The Algorithms)

#### A. خوارزمية تحليل التأثير (Impact Analysis Algorithm)
*   **المكان:** `TokenGraph.ts`
*   **الوظيفة:** لما تغير لون "البراند"، إزاي نعرف كل العناصر اللي هتتأثر؟
*   **الخوارزمية:** **DFS (Depth-First Search)**. النظام بيبني **Directed Graph** (رسم بياني موجه). لما تعدل Node، الـ DFS بيمشي ورا كل الخيوط (Dependencies) عشان يطلعلك قائمة بكل الـ Tokens "المصابة" بالتعديل ده، وده بيمنع أي كسر في السيستم.

#### B. علوم الألوان: `ColorScience`
*   **المكان:** `ColorScience.ts`
*   **الوظيفة:** الحسابات الرياضية للألوان مش مجرد RGB.
*   **الخوارزمية:** **CIEDE2000 Delta-E**. دي أدق خوارزمية رياضية في العالم لقياس "الفرق" بين لونين زي ما العين البشرية بتشوفه بالظبط. النظام بيحول الألوان لمساحة **CIE-L*a*b*** (رياضيات بحتة) عشان يضمن التباين (Contrast) والجمال.

#### C. محرك النوايا: `IntentEngine`
*   **المكان:** `IntentEngine.ts`
*   **الوظيفة:** فهم كلام اليوزر (Natural Language).
*   **الخوارزمية:** **Dual-Process Theory (System 1 & System 2)**:
    - **System 1 (سريع):** بيستخدم RegEx وهياكل ثابتة للتعرف الفوري على الأوامر المشهورة (زي "Rename").
    - **System 2 (بطيء/تحليلي):** لو الأمر معقد، بيشغل الـ LLM (LLM Analysis) عشان يحلل نية المستخدم العميقة.

---

### 🛠️ 4. أهم الدوال (Key Functions)

1.  **`processedThinking()`:** دي الدالة اللي في `AgentCore` اللي بتدير عملية التصميم بالذكاء الاصطناعي من الألف للياء.
2.  **`syncFromFigma()`:** دالة في `VariableManager` وظيفتها تعمل "مزامنة حية" بين بيانات فجما وبين الـ Memory بتاعة البلاقين عشان ميبقاش فيه تعارض.
3.  **`buildMappingPrompt()`:** دالة هندسية بتبني تعليمات (Instructions) معقدة جداً للذكاء الاصطناعي عشان يلتزم بـ JSON Schema محددة ومنظمة.

---

### 🛰️ 5. بروتوكول التواصل: "The Bridge"
البلاقين شغال بنظام **Asynchronous Message Passing**. الـ UI (React) بيبعت رسالة (Action) للـ Controller (Figma Side). الـ Controller بيشغل الـ "Capability" المطلوبة ويرجع النتيجة. ده بيخلي البلاقين سريع جداً ومبيعملش Freeze لبرنامج فجما وأنت شغال.

---

**الخلاصة:** Vibe مش مجرد أداة، ده **System of Intelligence** بيطبق أعلى معايير هندسة البرمجيات في قلب عالم التصميم. 🫡
